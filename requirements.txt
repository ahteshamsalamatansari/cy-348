# Carrier Browser Automation - Requirements
# Install with: pip install -r requirements.txt
#first we have to install requirements
playwright==1.57.0
flask==3.0.0
flask-cors==4.0.0


pandas
transformers[torch]
scikit-learn
requests
